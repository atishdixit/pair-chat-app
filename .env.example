# Used by docker-compose.yml (copy to ".env" in this same folder).
# Local non-Docker development uses backend/.env instead - see backend/.env.example.

CLIENT_ORIGIN=http://localhost:4000
JWT_SECRET=change-this-to-a-long-random-string

USER1_NAME=Alex
USER1_PASSWORD=change-me
USER2_NAME=Sam
USER2_PASSWORD=change-me
